import React, { useState, useEffect } from 'react';
import { ArrowRight, TrendingUp, Lock, Activity, Globe, Menu, X, CheckCircle } from 'lucide-react';

const Website = () => {
  const [lang, setLang] = useState('fr');
  const [isMenuOpen, setIsMenuOpen] = useState(false);
  const [scrolled, setScrolled] = useState(false);

  useEffect(() => {
    const handleScroll = () => {
      setScrolled(window.scrollY > 50);
    };
    window.addEventListener('scroll', handleScroll);
    return () => window.removeEventListener('scroll', handleScroll);
  }, []);

  const content = {
    fr: {
      nav: {
        method: "Le Protocole",
        proof: "Preuve",
        access: "Accès"
      },
      hero: {
        label: "SYSTEME D'EXPLOITATION POUR CLINIQUES",
        title: "Transformez l'intérêt patient en revenus prévisibles.",
        cta: "Candidater pour un Audit"
      },
      philosophy: {
        title: "La vente n'est pas une option. C'est une science.",
        text: "Les meilleurs chirurgiens ne laissent rien au hasard au bloc. Pourquoi le faites-vous en consultation ? Nous installons des protocoles rigoureux pour éliminer l'improvisation et sécuriser votre croissance.",
        cards: [
          { title: "Architecture", desc: "Construction d'un pôle vente autonome." },
          { title: "Contrôle", desc: "Monitoring précis des KPI financiers." },
          { title: "Exécution", desc: "Scripts et processus ne laissant aucune place à l'erreur." }
        ]
      },
      stats: {
        label: "RÉSULTATS VÉRIFIÉS",
        title: "Cas : Dr. A. Delobaux",
        items: [
          { value: "370K€", label: "CA Mensuel", sub: "vs 150K€ avant protocole" },
          { value: "+146%", label: "Croissance", sub: "en 60 jours" },
          { value: "72K€", label: "Ventes Vidéo", sub: "générés en 1 semaine" }
        ]
      },
      contact: {
        label: "ACCÈS RESTREINT",
        title: "Nous ne cherchons pas de clients.",
        subtitle: "Nous cherchons des partenaires prêts à scaler.",
        desc: "L'implémentation du protocole Sell It demande une rigueur absolue. Si vous êtes prêt à structurer votre clinique comme une entreprise de haut niveau, demandez votre audit.",
        cta: "Réserver l'Audit Stratégique"
      },
      footer: {
        rights: "Tous droits réservés."
      }
    },
    en: {
      nav: {
        method: "The Protocol",
        proof: "Proof",
        access: "Access"
      },
      hero: {
        label: "CLINIC OPERATING SYSTEM",
        title: "Turn patient interest into predictable revenue.",
        cta: "Apply for an Audit"
      },
      philosophy: {
        title: "Sales is not optional. It is a science.",
        text: "Top surgeons leave nothing to chance in the OR. Why do you do it in consultation? We install rigorous protocols to eliminate improvisation and secure your growth.",
        cards: [
          { title: "Architecture", desc: "Building an autonomous sales unit." },
          { title: "Control", desc: "Precise monitoring of financial KPIs." },
          { title: "Execution", desc: "Scripts and processes leaving no room for error." }
        ]
      },
      stats: {
        label: "VERIFIED RESULTS",
        title: "Case: Dr. A. Delobaux",
        items: [
          { value: "$370K", label: "Monthly Rev", sub: "vs $150K before protocol" },
          { value: "+146%", label: "Growth", sub: "in 60 days" },
          { value: "$72K", label: "Video Sales", sub: "generated in 1 week" }
        ]
      },
      contact: {
        label: "RESTRICTED ACCESS",
        title: "We are not looking for clients.",
        subtitle: "We are looking for partners ready to scale.",
        desc: "Implementing the Sell It protocol requires absolute rigor. If you are ready to structure your clinic like a high-level enterprise, request your audit.",
        cta: "Book Strategic Audit"
      },
      footer: {
        rights: "All rights reserved."
      }
    }
  };

  const t = content[lang];

  return (
    <div className="min-h-screen bg-black text-white font-sans selection:bg-white selection:text-black">
      <style>
        {`
          @import url('https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;500;600;700&family=Inter:wght@200;300;400;500&display=swap');
          .font-display { font-family: 'Space Grotesk', sans-serif; }
          .font-body { font-family: 'Inter', sans-serif; }
        `}
      </style>

      {/* Navbar */}
      <nav className={`fixed w-full z-50 transition-all duration-300 border-b ${scrolled ? 'bg-black/90 backdrop-blur border-zinc-800 py-4' : 'bg-transparent border-transparent py-8'}`}>
        <div className="container mx-auto px-6 flex justify-between items-center">
          {/* Logo */}
          <div className="text-2xl font-display font-bold tracking-tighter uppercase">
            SELL IT<span className="text-zinc-500">.</span>
          </div>

          {/* Desktop Nav */}
          <div className="hidden md:flex items-center space-x-12 text-xs font-medium tracking-widest uppercase">
            <a href="#philosophy" className="hover:text-zinc-400 transition-colors">{t.nav.method}</a>
            <a href="#results" className="hover:text-zinc-400 transition-colors">{t.nav.proof}</a>
            <button 
              onClick={() => setLang(lang === 'fr' ? 'en' : 'fr')} 
              className="flex items-center space-x-2 hover:text-zinc-400 transition-colors"
            >
              <Globe size={14} />
              <span>{lang}</span>
            </button>
            <a 
              href="#contact" 
              className="px-6 py-3 bg-white text-black font-bold hover:bg-zinc-200 transition-colors"
            >
              {t.nav.access}
            </a>
          </div>

          {/* Mobile Toggle */}
          <div className="md:hidden flex items-center space-x-6">
            <button onClick={() => setLang(lang === 'fr' ? 'en' : 'fr')} className="text-xs font-bold uppercase">{lang}</button>
            <button onClick={() => setIsMenuOpen(!isMenuOpen)}>
              {isMenuOpen ? <X /> : <Menu />}
            </button>
          </div>
        </div>

        {/* Mobile Menu */}
        {isMenuOpen && (
          <div className="absolute top-full left-0 w-full bg-black border-b border-zinc-800 p-8 flex flex-col space-y-6 text-center">
            <a href="#philosophy" onClick={() => setIsMenuOpen(false)} className="text-xl font-display uppercase tracking-widest">{t.nav.method}</a>
            <a href="#results" onClick={() => setIsMenuOpen(false)} className="text-xl font-display uppercase tracking-widest">{t.nav.proof}</a>
            <a href="#contact" onClick={() => setIsMenuOpen(false)} className="text-xl font-display uppercase tracking-widest text-zinc-400">{t.nav.access}</a>
          </div>
        )}
      </nav>

      {/* Hero Section */}
      <header className="relative pt-40 pb-20 md:pt-60 md:pb-40 px-6 border-b border-zinc-900">
        <div className="container mx-auto max-w-6xl">
          <div className="flex flex-col items-start">
            <div className="flex items-center space-x-3 mb-8">
              <div className="w-12 h-[1px] bg-white"></div>
              <span className="text-xs font-bold tracking-[0.2em] uppercase text-zinc-400">{t.hero.label}</span>
            </div>
            <h1 className="text-5xl md:text-8xl font-display font-medium leading-[0.9] mb-12 uppercase max-w-5xl">
              {t.hero.title}
            </h1>
            <a href="#contact" className="group flex items-center space-x-4 border-b border-white pb-1 hover:text-zinc-400 hover:border-zinc-400 transition-all">
              <span className="text-sm font-bold tracking-widest uppercase">{t.hero.cta}</span>
              <ArrowRight className="w-4 h-4 group-hover:translate-x-2 transition-transform" />
            </a>
          </div>
        </div>
        
        {/* Subtle grid background */}
        <div className="absolute inset-0 bg-[url('https://grainy-gradients.vercel.app/noise.svg')] opacity-20 pointer-events-none"></div>
      </header>

      {/* Philosophy / Grid Section */}
      <section id="philosophy" className="border-b border-zinc-900">
        <div className="grid grid-cols-1 md:grid-cols-2">
          {/* Text Side */}
          <div className="p-12 md:p-24 border-b md:border-b-0 md:border-r border-zinc-900 flex flex-col justify-center">
             <h2 className="text-3xl md:text-5xl font-display leading-tight mb-8">
               {t.philosophy.title}
             </h2>
             <p className="text-zinc-400 font-body text-lg leading-relaxed max-w-md">
               {t.philosophy.text}
             </p>
          </div>

          {/* Cards Side */}
          <div className="grid grid-rows-3 divide-y divide-zinc-900 bg-zinc-950">
            {t.philosophy.cards.map((card, idx) => (
              <div key={idx} className="p-12 flex flex-col justify-center hover:bg-zinc-900 transition-colors group cursor-default">
                <div className="flex justify-between items-baseline mb-2">
                  <h3 className="text-xl font-display uppercase tracking-wide group-hover:text-white transition-colors">{card.title}</h3>
                  <span className="text-zinc-700 font-display text-sm">0{idx + 1}</span>
                </div>
                <p className="text-sm text-zinc-500 font-body group-hover:text-zinc-300 transition-colors">{card.desc}</p>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Stats Section - High Contrast */}
      <section id="results" className="py-24 bg-white text-black border-b border-zinc-200">
        <div className="container mx-auto px-6">
          <div className="flex flex-col md:flex-row justify-between items-end mb-20 border-b border-black pb-6">
            <div>
              <span className="block text-xs font-bold tracking-[0.2em] uppercase mb-2">{t.stats.label}</span>
              <h2 className="text-4xl font-display uppercase">{t.stats.title}</h2>
            </div>
            <div className="mt-4 md:mt-0">
               <Activity className="w-8 h-8" />
            </div>
          </div>

          <div className="grid grid-cols-1 md:grid-cols-3 gap-12">
            {t.stats.items.map((item, idx) => (
              <div key={idx} className="flex flex-col">
                <span className="text-6xl md:text-8xl font-display font-medium tracking-tighter mb-4">
                  {item.value}
                </span>
                <span className="text-sm font-bold uppercase tracking-widest mb-1">{item.label}</span>
                <span className="text-zinc-500 text-xs font-body">{item.sub}</span>
              </div>
            ))}
          </div>
        </div>
      </section>

      {/* Contact Section */}
      <section id="contact" className="py-32 px-6 bg-black text-center relative overflow-hidden">
        <div className="container mx-auto max-w-3xl relative z-10">
          <span className="inline-block py-1 px-3 border border-zinc-800 rounded-full text-[10px] font-bold tracking-widest uppercase text-zinc-500 mb-8">
            {t.contact.label}
          </span>
          <h2 className="text-4xl md:text-6xl font-display uppercase mb-4 leading-tight">
            {t.contact.title}
          </h2>
          <h3 className="text-2xl md:text-3xl font-display text-zinc-500 mb-12">
            {t.contact.subtitle}
          </h3>
          <p className="text-zinc-400 mb-16 max-w-xl mx-auto font-body leading-relaxed">
            {t.contact.desc}
          </p>
          
          <a 
            href="mailto:contact@sellit.com" 
            className="inline-block bg-white text-black px-10 py-5 text-sm font-bold tracking-widest uppercase hover:bg-zinc-200 transition-all transform hover:scale-105"
          >
            {t.contact.cta}
          </a>
        </div>

        {/* Background Elements */}
        <div className="absolute top-0 left-1/2 -translate-x-1/2 w-[1px] h-full bg-gradient-to-b from-transparent via-zinc-800 to-transparent opacity-50"></div>
      </section>

      {/* Footer */}
      <footer className="py-12 border-t border-zinc-900 bg-black text-center md:text-left">
        <div className="container mx-auto px-6 flex flex-col md:flex-row justify-between items-center text-zinc-600 text-xs uppercase tracking-widest">
          <div className="mb-4 md:mb-0">
            <span className="text-white font-bold mr-2">SELL IT.</span>
            THE REVENUE PROTOCOL
          </div>
          <div>
            &copy; {new Date().getFullYear()} {t.footer.rights}
          </div>
        </div>
      </footer>
    </div>
  );
};

export default Website;
